import "tests/Simple/TopLevelParseImport.syl"

alias memory = byte ptr

func olChesnut(a: float, b: float, pred: bool) -> float{
    if pred then return a * b
    else return a / b
}

func memcpy(from: memory, to: memory, , size: u64){
    for i: u64 = 0; i < size; i++ do
        to[i] = from[i]
}